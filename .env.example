# Optional development tooling
ORIGINKIT_API_KEY=

# Runtime pooled connection and direct migration connection
DATABASE_URL=
MIGRATION_URL=

# Used only by the provider/CLI when needed; the application does not read it
SUPABASE_PASSWORD=

# Long random value shared by the Studio login and external write API
ADMIN_API_TOKEN=
